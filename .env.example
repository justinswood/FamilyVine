# FamilyVine Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to Git!

# ============================================================================
# Database Configuration
# ============================================================================
DB_HOST=db
DB_PORT=5432
DB_NAME=familytree
DB_USER=user
DB_PASSWORD=your_secure_database_password_here

# ============================================================================
# JWT Configuration
# ============================================================================
# IMPORTANT: Generate a secure random string for JWT_SECRET
# You can use: openssl rand -hex 64
JWT_SECRET=your_secure_jwt_secret_here_replace_with_long_random_string
JWT_EXPIRES_IN=8h

# ============================================================================
# CORS Configuration
# ============================================================================
CORS_ORIGIN=http://localhost:3030

# ============================================================================
# Node Environment
# ============================================================================
# Use 'development' for local testing, 'production' for deployment
NODE_ENV=production

# ============================================================================
# Email Configuration (Gmail SMTP)
# ============================================================================
# Optional: Leave empty to disable email functionality
# Emails will be logged instead if not configured
#
# To enable email:
# 1. Use your Gmail account or any SMTP service
# 2. For Gmail: Create an App Password at https://myaccount.google.com/apppasswords
# 3. Enable 2-Step Verification first if using Gmail
# 4. Fill in the values below

EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-gmail-app-password-here
EMAIL_FROM=your-email@gmail.com
FRONTEND_URL=http://localhost:3030

# ============================================================================
# Alternative Email Services
# ============================================================================
# You can use other SMTP services by changing EMAIL_SERVICE:
#
# Outlook/Hotmail:
#   EMAIL_SERVICE=hotmail
#   EMAIL_USER=your-email@outlook.com
#   EMAIL_PASSWORD=your-password
#
# Yahoo:
#   EMAIL_SERVICE=yahoo
#   EMAIL_USER=your-email@yahoo.com
#   EMAIL_PASSWORD=your-password
#
# Custom SMTP (advanced):
#   See backend/services/emailService.js for custom configuration

# ============================================================================
# Notes
# ============================================================================
# - Replace ALL placeholder values before running
# - Keep this file secure and never commit it to Git
# - The .env file should be in .gitignore
# - See README.md for detailed setup instructions
